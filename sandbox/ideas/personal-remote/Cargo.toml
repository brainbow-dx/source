[package]
name = "prem-rs"
version = "0.0.0"
edition = "2024"

[features]
default = ["std"]
std = []
dev = []
inspect = []
profiler = []

[dependencies]
eyre = { version = "*", default-features = false }
