using UnityEngine;

// A component given to every object spawned by the map generator.
// Useful for despawning objects between iterations and after the round has ended.
public class MapGenSpawnedObject : MonoBehaviour
{
}
