[template]
name = "app"
include = [
    "Cargo.toml",
    "src/**/*.rs",
    "deno.json",
    "scripts/**/*.tsx",
    "lib/**/*.dart",
    "pubspec.yaml",
    "README.md",
]
# .. or
# exclude = [
#     ".output/",
#     ".scratch/",
# ]

[hooks.generate]
command = "deno run -A scripts/.generate.tsx"

# [hooks.error]
# command = "deno run scripts/.cleanup.tsx"
