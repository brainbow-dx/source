[package]
name = "ethos-lua"
version = "0.1.0"
edition = "2024"

[dependencies]
ethos-core = { workspace = true }
mlua = { workspace = true, features = ["lua54", "vendored"] }

[dev-dependencies]
eyre = { workspace = true }
