info:
  name: did
  type: http
  seq: 1

http:
  method: GET
  url: https://sketch.brainbow.localhost/.well-known/did.json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
